
/**
 * Enables creating ratelimits for actions  
 * 
 */
module.exports = class TimeHandler {
  /**
   * Constructor a new Client  
   */
  constructor (options) {
    const ws = require('ws')
    var a = 1
  }
}
